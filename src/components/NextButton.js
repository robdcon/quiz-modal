import React, { Component } from 'react'


const NextButton = (props) =>
{
	
		return(

			
			<div>			
				<button className="btn next" onClick={props.handler}>NEXT</button>
			</div>

		)
	
}

export default NextButton